export const Seeds = (() => {
  async function run() {
    // Dictaphone seeds
    // Clips are created through the recording interface
    console.log("Dictaphone ready - start recording!")
  };

  return {run}
})()