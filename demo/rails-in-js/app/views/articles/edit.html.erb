<h1>Edit Article</h1>

<% if @article.errors && @article.errors.length > 0 %>
  <div class="errors">
    <ul>
      <% @article.errors.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<form onsubmit="return updateArticle(event, <%= @article.id %>)">
  <p>
    <label>Title:</label>
    <input type="text" id="title" value="<%= @article.title %>" required>
  </p>
  <p>
    <label>Body:</label>
    <textarea id="body" required><%= @article.body %></textarea>
  </p>
  <button type="submit">Update Article</button>
</form>

<p>
  <a onclick="navigate('/articles/<%= @article.id %>')">Cancel</a> |
  <a onclick="navigate('/articles')">Back to Articles</a>
</p>
