<h1>Edit Article</h1>

<% if @article.errors && @article.errors.length > 0 %>
  <div class="errors">
    <ul>
      <% @article.errors.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<form onsubmit="event.preventDefault(); updateArticle(event, <%= @article.id %>); return false">
  <p>
    <label>Title:</label>
    <input type="text" name="title" value="<%= @article.title %>" required>
  </p>
  <p>
    <label>Body:</label>
    <textarea name="body" required><%= @article.body %></textarea>
  </p>
  <button type="submit">Update Article</button>
</form>

<p>
  <a onclick="navigate('/articles/<%= @article.id %>')">Cancel</a> |
  <a onclick="navigate('/articles')">Back to Articles</a>
</p>
