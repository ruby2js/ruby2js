# Database configuration
# Adapter is selected at build time, not runtime
# Override with DATABASE=<adapter> environment variable

development:
  adapter: dexie
  database: ruby2js_rails_dev

production:
  adapter: dexie
  database: ruby2js_rails

# Alternative: sql.js for full SQL support (larger bundle ~2.7MB vs ~50KB)
# development:
#   adapter: sqljs
#   database: ruby2js_rails_dev
